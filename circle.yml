deployment:
  staging:
    branch: master
    commands:
      - git push https://git.heroku.com/eaglesflight-reloaded.git
      - heroku run rake db:migrate --app eagles-flight
machine:
  ruby:
    version: 2.2.3
