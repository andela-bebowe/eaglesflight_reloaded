<div class="row">
  <div class="col-sm-6">
    <h5 class="text-center">From:  <%=dept(flights)%></h5>
  </div>
  <div class="col-sm-6">
    <h5 class="text-center">To:  <%=dest(flights)%></h5>
  </div>
</div>
<table class="table table-hover table-responsive">
  <thead>
  <tr>
      <th>Pick</th>
      <th>Airlines</th>
      <th>Planes</th>
      <th>Date of Departure</th>
      <th>Time of Departure</th>
      <th>Price for <%=num%></th>
  </tr>
</thead>
  <tbody>
      <%flights.each do |flight|%>
      <%(flight.planes).each do |plane|%>
        <%airline = plane.airline%>
        <tr>
          <td><%=radio_button_tag :plane_id, plane.id %></td>
          <td><%=airline%></td>
          <td><%=plane%></td>
          <td><%=date(flight)%></td>
          <td><%=time(flight)%></td>
          <td><%=get_cost(airline, flight, num)%></td>
        </tr>
      <%end%>
    <%end%>
  </tbody>
</table>
